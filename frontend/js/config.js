// Central place to point the frontend at the backend.
// Jab backend deploy karo (e.g. Render/Railway/VPS), yaha URL change kar dena.
// Local development me FastAPI default 8000 port par chalta hai.
const API_BASE_URL = "http://localhost:8000";
